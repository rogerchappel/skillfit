{
  "name": "skillfit",
  "version": "0.1.0",
  "description": "Local-first readiness checker for reusable agent skills.",
  "type": "module",
  "bin": { "skillfit": "./bin/skillfit.js" },
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check.js",
    "build": "node scripts/build.js",
    "smoke": "node bin/skillfit.js fixtures/valid-skill --format markdown --out tmp/smoke.md && test -s tmp/smoke.md"
  },
  "keywords": ["agent-skills", "cli", "readiness", "lint", "openclaw"],
  "author": "Roger Chappel",
  "license": "MIT",
  "engines": { "node": ">=18" }
}
