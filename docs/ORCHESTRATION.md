# Orchestration

Agents should run `skillfit <skill-dir>` before proposing a skill for reuse. Use JSON output in CI and Markdown output for human review. Treat findings marked `fail` as blockers and `warn` as review notes.
